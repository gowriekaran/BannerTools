console.log("script loaded");

$(document).ready(function () {
    // chrome.storage.sync.get(null, function (items) {
    //     if (items["_BT_adContainer"]) {
    //         $("#_BT_adContainer").val(items["_BT_adContainer"]);
    //     }
    //     if (items["_BT_replayButton"]) {
    //         $("#_BT_replayButton").val(items["_BT_replayButton"]);
    //     }
    // });

    // if (localStorage["_BT_adContainer"]) {
    //     _BT_adContainer = localStorage["_BT_adContainer"];
    //     $("#_BT_adContainer").val(_BT_adContainer);
    // } else {
    //     console.log(localStorage["_BT_adContainer"]);
    // }
    // if (localStorage["_BT_replayButton"]) {
    //     _BT_replayButton = localStorage["_BT_replayButton"];
    //     $("#_BT_replayButton").val(_BT_replayButton);
    // } else {
    //     console.log(localStorage["_BT_replayButton"]);
    // }

    // $("#_BT_settingsSaveButton").click(function () {
    //     if ($("#_BT_adContainer").val()) {
    //         setToStorage({ "_BT_adContainer": $("#_BT_adContainer").val() });
    //     }
    //     if ($("#_BT_replayButton").val()) {
    //         setToStorage({ "_BT_replayButton": $("#_BT_replayButton").val() });
    //     }

    //     location.reload();
    // });

    // function setToStorage(obj) {
    //     chrome.storage.sync.set(obj);
    // }

});